# 变更背景/目的

在登录页增加进入“微信登录获取手机号 Demo”页面的快捷入口，减少联调时手动输入路径的成本。

# 变更文件清单

- `YiSmartCloud-member-uniapp/pages/login/index.vue`

# 关键改动说明

- 在登录卡片区域新增按钮“进入微信手机号Demo”。
- 新增 `goPhoneDemo` 方法，通过 `uni.navigateTo` 跳转至 `/pages/test/wechat-phone-demo`。
- 增加按钮样式 `demo-btn`，与现有登录按钮区分展示。

# 影响范围（接口、数据库、前端、兼容性）

- 前端：仅登录页 UI 与跳转行为增加，不影响原登录逻辑。
- 接口：无变更。
- 数据库：无变更。
- 兼容性：无破坏性影响。

# 验证方式（已执行/未执行及原因）

- 已执行：针对 `pages/login/index.vue` 的静态诊断检查，无新增告警。
- 未执行：微信开发者工具中的实际点击跳转验证（当前会话未直接运行小程序）。
