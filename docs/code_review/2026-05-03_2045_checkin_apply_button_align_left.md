# 入住列表「发起入住申请」按钮左对齐

## 变更背景/目的

工具栏主按钮靠右与表格首列视觉脱节，改为靠左更符合操作习惯。

## 变更文件清单

- `YiSmartCloud-front/src/views/serve/checkIn/index.vue`

## 关键改动说明

- `el-row` 的 `justify` 由 `end` 改为 `start`，使「发起入住申请」与左侧内容对齐。

## 影响范围

- 仅入住登记列表页工具栏布局。

## 验证方式

- 未执行自动化测试；浏览器打开入住办理列表确认按钮在左侧。
