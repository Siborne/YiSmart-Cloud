---
description: Enforce UTF-8 encoding for all created files
alwaysApply: true
---

# 文件编码统一规则（UTF-8）

- 所有新建或修改的代码/配置/文档文件，编码必须使用 UTF-8。
- 禁止使用 GB2312、GBK、ANSI、Big5、UTF-16 等其它编码保存文件。
- 若发现文件出现乱码、中文异常或编码不一致，优先将该文件转换为 UTF-8 后再继续改动。
- 在批量生成文件、脚本写文件或模板输出文件时，也必须显式确保输出为 UTF-8。
- 提交前如涉及中文内容，需确认在 IDE 与 git diff 中显示正常，避免编码回退。
